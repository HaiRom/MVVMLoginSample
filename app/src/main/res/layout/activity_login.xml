<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
>
    <data>
        <variable name="loginViewModel" type="com.example.mvvmsample.login.LoginViewModel"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout

            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/activityLogin"
            tools:context=".login.LoginActivity">

        <EditText
                android:id="@+id/editTextUserName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:inputType="textPersonName"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_marginLeft="30dp"
                android:layout_marginRight="30dp"
                android:hint="Enter user name"
                android:text="@={loginViewModel.liveDataUser}"
                android:ems="10"/>

        <EditText
                android:id="@+id/editTextPassWord"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:inputType="textPassword"
                app:layout_constraintLeft_toLeftOf="@id/editTextUserName"
                app:layout_constraintRight_toRightOf="@id/editTextUserName"
                app:layout_constraintTop_toBottomOf="@id/editTextUserName"
                android:layout_marginTop="20dp"
                android:hint="Enter your password"
                android:text="@={loginViewModel.liveDataPass}"
                android:ems="10"/>
        <Button
                android:id="@+id/btnLogin"
                android:text="LOGIN"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/editTextPassWord"
                android:layout_marginTop="20dp"
                android:layout_width="wrap_content"
                android:onClick="@{loginViewModel::onClickBtnLogin}"
                android:layout_height="wrap_content"
        />

        <ProgressBar
                android:id="@+id/progressCircular"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
        />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
